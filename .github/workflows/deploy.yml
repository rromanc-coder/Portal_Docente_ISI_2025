name: 🚀 Deploy Portal ISI

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: 📦 Checkout del repositorio
        uses: actions/checkout@v4

      - name: 🚀 Desplegando aplicación
        run: |
          echo "📂 Sincronizando código con /srv/pln/proyecto_ISI..."
          DEPLOY_PATH="/srv/pln/proyecto_ISI"

          # Copiar contenido actualizado del repo al contenedor de despliegue
          sudo rsync -av --delete \
            --exclude 'runner_portal' \
            --exclude '.git' \
            --exclude '.github' \
            ./ $DEPLOY_PATH/

          cd $DEPLOY_PATH
          echo "🔁 Reconstruyendo contenedores..."
          sudo docker compose down || true
          sudo docker compose up -d --build
